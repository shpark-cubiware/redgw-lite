# ─── 코어 프레임워크 ─────────────────────────────────────────
fastapi==0.136.3                # ASGI 웹 프레임워크 (starlette 1.2.x 기반). 0.131+ 내장 Pydantic(Rust) JSON 직렬화 — 커스텀 응답클래스 불필요
gunicorn==25.3.0                # WSGI/ASGI 서버 (HTTP/2 베타, 네이티브 ASGI 워커, Dirty Arbiter)
uvicorn[standard]==0.44.0       # ASGI 워커 (gunicorn -k uvicorn.workers.UvicornWorker)
# ─── 데이터 / 직렬화 ────────────────────────────────────────
redis[hiredis]==7.4.0           # Redis 비동기 클라이언트 (hiredis C 파서 포함)
pyyaml==6.0.3                   # YAML 설정 파싱 (config.yaml)
pydantic==2.13.4                # 요청/응답 스키마 검증 (V2). 2.13.x Rust 직렬화로 FastAPI가 dict 반환을 고속 직렬화
pydantic-settings==2.13.1       # 환경변수 오버라이드 설정
# ─── HTTP / 보안 ────────────────────────────────────────────
httpx==0.28.1                   # 비동기 HTTP 클라이언트 (FastAPI TestClient 의존)
# Rate Limiting: 실집행은 Nginx limit_req(10r/s)가 담당 — 앱 레벨 slowapi 제거(미집행 죽은 코드였음)
# ─── 로깅 ───────────────────────────────────────────────────
# 운영 로그는 plain 고정 (사람 실시간 인지 우선), 감사 로그는 stdlib json — 외부 JSON 라이브러리 불필요

# ─── 테스트 ──────────────────────────────────────────────────
pytest==9.0.3                   # 테스트 프레임워크
pytest-asyncio==1.3.0           # pytest 비동기 테스트 지원 (session 스코프 이벤트 루프)
websockets==16.0                # WebSocket 클라이언트 (uvicorn[standard] + Pub/Sub 테스트)
# ─── transitive 핀 — 빌드 재현성 ────────────────────────────
# 직접 import하지 않지만, 메이저 버전이 바뀌면 동작이 달라질 수 있어 명시적 핀
anyio==4.13.0                   # pytest-asyncio, starlette 의존
starlette==1.2.0                # fastapi 0.136.3 transitive 의존 — 메이저 변경 시 ASGI 동작 회귀 방지용 재현성 핀
